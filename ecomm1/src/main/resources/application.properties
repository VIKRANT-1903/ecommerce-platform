spring.application.name=ecomm1

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://ep-aged-mountain-ah172dvu-pooler.c-3.us-east-1.aws.neon.tech/ecommerce_auth?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_jyx4alqzcIQ5
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
# With Flyway omitted, let Hibernate manage schema in dev environments.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://kshitijsingh:qwerty123@cluster0.u51pwoe.mongodb.net/?appName=Cluster0}
spring.data.mongodb.database=${MONGODB_DATABASE:productdb}

# JWT Configuration
jwt.secret=${JWT_SECRET:your-256-bit-secret-key-change-this-in-production-minimum-32-characters}
jwt.expiration=${JWT_EXPIRATION:86400000}

# External Services
external.inventory.service.url=${INVENTORY_SERVICE_URL:https://ecomm-f8qn.onrender.com}

# Server Configuration
server.port=${SERVER_PORT:8082}
server.error.include-message=always
server.error.include-binding-errors=always

# Logging Configuration
logging.level.root=INFO
logging.level.com.example.ecomm1=DEBUG
logging.level.org.springframework.security=DEBUG
